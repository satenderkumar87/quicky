# quicky

**Note:** This project requires **Node.js v22**. Please ensure you are using Node.js version 22 before installing dependencies or running the project.

## Getting Started

### Prerequisites
- [Node.js v22](https://nodejs.org/)
- npm (comes with Node.js)

### Installation
1. Clone the repository:
   ```bash
   git clone <repository-url>
   cd quicky
   ```
2. Install dependencies:
   ```bash
   npm install
   ```

### Running the App
To start the development server:
```bash
npm start
```
The app will be available at [http://localhost:3000](http://localhost:3000).

### Available Scripts
- `npm start` – Runs the app in development mode.
- `npm run build` – Builds the app for production.
- `npm test` – Launches the test runner.
- `npm run eject` – Ejects the app (not reversible).

## Project Structure
```
quicky/
  public/           # Static files and HTML template
  src/              # React components and source code
    App.js          # Main App component
    index.js        # Entry point
    ...             # Other supporting files
  package.json      # Project metadata and scripts
  README.md         # Project documentation
```

## License
This project is open source and available under the [MIT License](LICENSE) (add a LICENSE file if needed).
